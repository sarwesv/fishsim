name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - claude/retro-fish-tank-game-b5wfv0
  workflow_dispatch:

# Allow the workflow to publish to Pages
permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; don't cancel an in-progress publish
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        # enablement: true turns Pages on if it isn't already, so no manual
        # Settings toggle is needed for the first deploy.
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload site
        # The site is static at the repo root — upload it as-is.
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
